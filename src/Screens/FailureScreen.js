import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const FailureScreen = () => {
    return (
        <View>
            <Text>Failure Screen</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default FailureScreen;
